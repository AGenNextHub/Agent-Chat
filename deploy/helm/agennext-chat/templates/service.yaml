apiVersion: v1
kind: Service
metadata:
  name: {{ include "agennext-chat.fullname" . }}
  labels:
    {{- include "agennext-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "agennext-chat.selectorLabels" . | nindent 4 }}
